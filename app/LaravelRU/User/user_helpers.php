<?php
/*
|--------------------------------------------------------------------------
| Helpers
|--------------------------------------------------------------------------
*/

HTML::macro('social', function ($key, $user)
{
	if ($info = $user->social->{$key})
	{
		return $info;
	}
});
